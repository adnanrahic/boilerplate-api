version: "3"
services:
  mongo:
    image: mongo
    volumes:
      - mongo:/data/db
    networks: 
      backend:
        aliases:
          - mongo
    ports:
      - "27017:27017"
  
  api:
    environment:
      - API_IMAGE=adnanrahic/boilerplate-api
    build:
      context: ./
      dockerfile: Dockerfile.test-unit
    image: "${API_IMAGE}:test-unit"
    volumes:
      - ./logs:/usr/src/app/logs
    networks: 
      - backend
    depends_on:
      - mongo
    ports:
      - "3000:3000"

networks: 
  backend:

volumes:
  mongo: